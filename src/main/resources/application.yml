spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sti?serverTimezone=Asia/Seoul
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
logging:
  level:
    org.hibernate.SQL: debug
    #logging.level.com.god.bo.test=info
    #logging.level.com.god.bo.test.controller=debug
    #각 패키지에 디폴트 로깅 레벨을 지정할 수 있다.